# 如何从组装的基因组中鉴定rDNA array

从**组装好的基因组中鉴定 rDNA array（核糖体 DNA 串联重复区）**是一个**高度专业、同时也是审稿人非常关注**的问题，尤其在 **T2T 组装、着丝粒/端粒、重复序列进化**研究中。下面我给你一套**从“快速定位”到“精确定界”的完整方案**，并区分**常规组装 vs 高质量 T2T 组装**两种情况。



## 一、rDNA array 的基本特征（先统一概念）

### 1️⃣ rDNA 的组成（真核核基因组）

* **45S rDNA repeat unit**

  ```
  [18S] – ITS1 – [5.8S] – ITS2 – [28S]
  ```
* 重复单位长度：

  * 植物：~9–10 kb
  * 动物：~43 kb（含 IGS）

### 2️⃣ 典型特征

| 特征   | 描述          |
| ---- | ----------- |
| 结构   | 高度串联重复      |
| 拷贝数  | 数十–数百       |
| GC   | 偏高          |
| 位置   | 常靠近着丝粒或端粒   |
| 组装难点 | 短读段几乎无法正确组装 |



## 二、快速筛选：在基因组中定位 rDNA 候选区域（必做）

### 方法 1 ⭐⭐⭐⭐⭐：用已知 rDNA 序列 BLAST

#### Step 1：准备 rDNA 查询序列

可选：

* 18S rRNA
* 28S rRNA
* 完整 45S repeat

（来自 SILVA / NCBI）

#### Step 2：BLAST 到组装基因组

```bash
makeblastdb -in genome.fa -dbtype nucl

blastn -query 18S.fa \
       -db genome.fa \
       -evalue 1e-20 \
       -outfmt 6 \
       > rDNA.blast.txt
```

#### 判断 rDNA array 的标准

* 命中：

  * **同一染色体**
  * **密集排列**
  * 间距 ≈ 重复单位长度
* 命中次数 ≫ 普通基因

👉 **密集簇 = rDNA array**



## 三、结构层面确认：串联重复特征

### 方法 2 ⭐⭐⭐⭐：Tandem Repeat Finder (TRF)

```bash
trf genome.fa 2 7 7 80 10 50 500 -d -h
```

筛选：

* repeat size ≈ rDNA 单元长度
* copy number 高



## 四、覆盖度证据（非常关键，审稿人爱看）

### 方法 3 ⭐⭐⭐⭐⭐：测序深度异常

#### 原理

rDNA copy number 高 → **reads 覆盖度显著高于全基因组平均**

#### 做法

```bash
minimap2 -ax map-hifi genome.fa reads.fq | samtools sort -o aln.bam
samtools depth aln.bam > depth.txt
```

* rDNA 区域：

  * depth ≫ genome average（10–100×）



## 五、在高质量 / T2T 组装中的精确定界（推荐）

### 方法 4 ⭐⭐⭐⭐⭐：自比对（dotplot）

```bash
minimap2 -x asm5 genome.fa genome.fa > self.paf
```

用 **dotplot**（如 mummerplot）查看：

* 对角线旁出现：

  * **密集平行斜线**
  * 对应 rDNA array



## 六、读段层面验证（尤其适合长读段）

### 方法 5 ⭐⭐⭐⭐⭐：直接从 reads 识别 rDNA

```bash
blastn -query 28S.fa -subject reads.fq
```

* 一个 read 命中多个 rDNA 单元
* 可估计：

  * 单元长度
  * 拷贝数
  * 变异



## 七、拷贝数估计（常被问）

### 方法 6：覆盖度法

```text
copy_number ≈ (rDNA depth) / (genome depth)
```



## 八、与染色体结构关联（高水平论文）

### 常见位置关系

* rDNA ↔ 着丝粒卫星
* rDNA ↔ NOR（核仁组织区）
* rDNA ↔ 断裂/重排热点

👉 在比较基因组中极有价值



## 九、不同质量组装的策略差异（很重要）

| 组装类型          | rDNA 状态    |
| ------------- | ---------- |
| Illumina-only | 多数崩塌       |
| HiFi + ONT    | 可部分展开      |
| T2T           | 连续完整 array |



## 十、审稿人常问的 5 个问题（提前准备）

1. **How was the rDNA array identified?**
2. **Is the array continuous or collapsed?**
3. **How many copies are present?**
4. **What is the sequence variation among copies?**
5. **Is the rDNA locus conserved across species?**



## 十一、论文级一句话方法描述（可直接用）

> *rDNA arrays were identified by BLAST searches using conserved 18S and 28S rRNA sequences, followed by coverage analysis and tandem repeat detection to confirm their repetitive structure and genomic organization.*



## 如果你愿意，我可以进一步帮你：

* 🔹 **针对植物 / 哺乳动物 / 昆虫给定制方案**
* 🔹 **判断你现在的组装是否发生 rDNA collapse**
* 🔹 **做 rDNA array 的进化比较**
* 🔹 **帮你写 Methods + Figure 设计**

你可以直接告诉我：
👉 **你的物种？组装水平（HiFi/ONT/T2T）？是否已有原始 reads？**


---

# 如何从头识别基因组中的rDNA array


这是一个**方法学层面很“硬核”的问题**，也是在 **没有先验 rDNA 序列**、或者做 **比较基因组 / 新物种 / 审稿人严格** 情况下，**最标准的“从头（de novo）识别 rDNA array”方案**。下面我会按**“识别逻辑 → 可落地流程 → 关键判据 → 不同数据条件 → 审稿人视角”**系统讲清楚。

---

# 一、从头识别 rDNA array 的核心思想

> **不依赖已知 rDNA 序列，而是依靠 rDNA 的“结构与信号特征”自动识别**

rDNA array 的**四大“指纹”**：

1. **极高拷贝数的串联重复**
2. **重复单元长度高度一致（~10 kb 或 ~40 kb）**
3. **覆盖度显著高于基因组平均**
4. **内部含高度保守的 rRNA 结构域**

---

# 二、标准的 de novo 识别总体流程（推荐）

## Step 0：准备数据（决定上限）

| 数据       | 要求                  |
| -------- | ------------------- |
| 组装基因组    | contig / chromosome |
| 原始 reads | HiFi / ONT（强烈推荐）    |

> ⚠️ **只用组装结果，准确性有限；加上 reads 才能“封顶”**



## Step 1 ⭐⭐⭐⭐⭐

## 全基因组扫描“长串联重复”

### 工具 1：Tandem Repeat Finder (TRF)

```bash
trf genome.fa 2 7 7 80 10 50 500 -d -h
```

筛选规则：

* repeat unit length：

  * 真核常见：5–50 kb
* copy number：

  * ≥10
* 连续 repeat span：

  * ≥100 kb

👉 **满足这三点的区域 = 强候选 rDNA arr

## Step 2 ⭐⭐⭐⭐

## 覆盖度异常检测（de novo 核心证据）

### 原理

rDNA 高拷贝 → reads 深度异常升高

```bash
minimap2 -ax map-hifi genome.fa reads.fq | samtools sort -o aln.bam
samtools depth aln.bam > depth.txt
```

判断标准：

* rDNA 候选区：

  * depth ≥ 5–20 × genome average

📌 **这是不依赖序列内容的强证据**



## Step 3 ⭐⭐⭐⭐

## 组装自比对（dotplot）

### 方法

```bash
minimap2 -x asm5 genome.fa genome.fa > self.paf
```

在 dotplot 中观察：

* 大块：

  * 平行对角线
* 多条：

  * 等间距排列

👉 这是串联重复的经典几何信号



## Step 4 ⭐⭐⭐

## rRNA 结构域的“弱先验”验证（非必需但很稳）

即便是 de novo，**也可以用“结构模型”而不是具体序列**。

### 方法

* Infernal + Rfam covariance model
* 搜索：

  * 18S / 28S rRNA 结构

```bash
cmscan --cut_ga Rfam.cm genome.fa
```

👉 命中集中于同一区域 = rDNA array

📌 **这一步不算“序列先验”**



# 三、在 reads 层面的 de novo 验证（非常强）

## Step 5 ⭐⭐⭐⭐⭐

## 直接从 reads 发现 rDNA repeats

### 1️⃣ k-mer 频谱分析

* rDNA 单元 k-mer：

  * 出现频率极高
* 工具：

  * Jellyfish
  * KMC

```bash
jellyfish count -m 31 reads.fq
jellyfish histo mer_counts.jf
```

异常高频 k-mer → 来自 rDNA



### 2️⃣ read self-alignment

* 单条 read 内出现：

  * 多次高度相似区块
* 说明：

  * 跨越多个重复单元



# 四、如何区分 rDNA array vs 其他重复？

| 特征   | rDNA   | 卫星 DNA   |
| ---- | ------ | -------- |
| 单元长度 | kb 级   | bp–几百 bp |
| 内部结构 | rRNA 域 | 无        |
| GC   | 偏高     | 常偏低      |
| 覆盖度  | 极高     | 高但不稳定    |
| 功能   | 转录活跃   | 多为结构     |



# 五、不同组装质量下的策略差异

## 1️⃣ contig 级组装

* rDNA 往往：

  * 在 contig 末端
  * 或 collapse

👉 只能：

* 定位
* 估计 copy number



## 2️⃣ 染色体级 / T2T

* 可识别：

  * 完整 array
  * 边界
  * 单元变异


# 六、审稿人视角：de novo 的“合格标准”

审稿人通常接受以下逻辑链：

1. **Genome-wide tandem repeat scan**
2. **Depth enrichment**
3. **Self-alignment patterns**
4. **rRNA structural evidence**

> 即使不用 BLAST 已知 rDNA 序列，也是**完全合格的 de novo 识别**



# 七、论文级一句话总结（可直接用）

> *rDNA arrays were identified de novo based on their characteristic tandem repeat organization, significantly elevated sequencing depth, and the presence of conserved rRNA structural domains, without relying on prior rDNA sequence information.*



# 八、现实建议（非常重要）

* **NSFC / 普通期刊**
  → TRF + depth + dotplot
* **Genome Biology / T2T 级别**
  → reads 层面验证 + 图结构解析
* **比较进化**
  → 单元长度、copy number、变异



## 如果你愿意，我可以继续：

* 🔹 **帮你设计“完全不依赖先验”的 pipeline**
* 🔹 **判断你现在的数据是否足以 de novo 识别**
* 🔹 **帮你区分 rDNA vs 着丝粒卫星**
* 🔹 **写 Methods + Figure 方案**

你可以直接告诉我：
👉 **物种？组装质量？是否有原始 HiFi/ONT reads？**




